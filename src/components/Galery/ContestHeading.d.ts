import { FC } from "react";
interface ContestHeadingProps {
    title: string;
    deadline: string;
}
declare const WeddingTitleAndTimer: FC<ContestHeadingProps>;
export default WeddingTitleAndTimer;
